{
  "name": "samples",
  "version": "1.0.0",
  "description": "dummy",
  "main": "index.js",
  "scripts": {
    "test": "jest --collect-coverage --ci --reporters=default --reporters=jest-junit",
    "watch-test": "jest --collect-coverage --watchAll",
  },
  "author": "Nicolas",
  "license": "ISC",
  "dependencies": {
    "@hubspot/cms-cli": "^1.1.7",
    "chalk": "^4.0.0",
    "debug": "^4.1.1",
    "jest": "^26.0.1",
    "jest-junit": "^10.0.0",
    "node-sass": "^4.14.1"
  }
}
